# Frontend
# Em desenvolvimento, a UI (npm run dev) precisa apontar para o backend local do Vercel.
# Em produção (Vercel), deixe vazio ou use a URL https://seu-app.vercel.app
VITE_API_BASE_URL=http://localhost:3000

# Backend (Serverless)
AUTH_ALLOWED_USER=adv
# Gera com: node -e "console.log(require('bcryptjs').hashSync('sua-senha', 12))"
AUTH_PASSWORD_HASH=coloque_aqui_o_hash_bcrypt
# Use uma string longa/aleatória (ex.: openssl rand -base64 32)
JWT_SECRET=defina_uma_chave_bem_secreta
JWT_EXPIRES_IN=24h

# Integração GitHub (necessário para CRUD de artigos)
GITHUB_OWNER=seu-usuario
GITHUB_REPO=seu-repositorio
GITHUB_BRANCH=main
GITHUB_FILE_PATH=src/data/articles/index.ts
GITHUB_TOKEN=ghp_seu_token_com_acesso

# CORS
# Local: porta configurada no Vite (padrão 8085). Produção: https://seu-app.vercel.app
CORS_ORIGIN=http://localhost:8085
